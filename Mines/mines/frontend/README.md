# Mines Frontend

React + Vite UI for the Mines game.

```bash
npm install
npm run dev
```

Requires the Django API at `http://127.0.0.1:8000/api/` (see `../backend` in this game folder).

Optional: set `VITE_API_URL` to point at another API base URL.
